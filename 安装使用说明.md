# UDA — 用户安装与使用说明

## 系统要求

- Chrome 88+ 或 Edge 全版本
- 开发者模式（用于加载未上架的扩展程序）
- 一个兼容 OpenAI 格式的大模型 API 密钥（支持 OpenAI、DeepSeek、通义千问等）

## 安装步骤

1. **解压 ZIP 包**
   将 `LocalDebugAI_v1.0.zip` 解压到一个**无中文、无空格**的目录（如 `~/Extensions/ai-debug-assistant/`）

2. **打开扩展管理页面**
   浏览器地址栏输入并打开：
   ```
   chrome://extensions/
   ```

3. **开启开发者模式**
   点击右上角的「开发者模式」开关（处于开启状态）

4. **加载扩展**
   点击「加载已解压的扩展程序」→ 选择解压后的文件夹 → 完成安装

5. **固定到工具栏**
   点击浏览器工具栏的拼图图标 → 找到「UDA」→ 点击图钉固定

## 使用流程

### 第一步：配置 API 密钥
1. 点击插件图标打开弹窗
2. 切换到 **「设置中心」** 标签页
3. 填写你的 API 密钥、接口地址（如 `https://api.openai.com/v1`）和模型名称
4. 点击「连通性测试」校验配置是否有效
5. 点击「保存配置」

### 第二步：绑定项目需求文档（可选但推荐）
1. 切换到 **「项目上下文」** 标签页
2. 上传 `.txt` 或 `.md` 格式的需求文档，或在文本域中粘贴内容
3. 点击「保存文档」
   > 绑定后 AI 可结合业务需求校验交互合规性，否则仅排查代码错误

### 第三步：调试录制
1. 打开要调试的页面（任意 http/https 站点，或本地项目）
2. 打开插件，确保状态栏显示为绿色「已填写项目简介」（如已填写）
3. 点击 **「开始录制」**
4. 在你的页面上复现 BUG 操作（点击、路由跳转等）
5. 录制过程中，弹窗会实时展示捕获的操作日志
6. 录制完成后点击 **「停止录制」**

### 第四步：AI 分析
1. 点击 **「一键 AI 分析本次录制日志」**
2. AI 分析完成后会展示：
   - **异常根因汇总**：标注业务异常/代码异常类型
   - **完整修复方案**：可直接复制的代码片段
3. 点击代码右上角「复制」按钮复制到项目中使用

### 第五步：查看历史
- 每次 AI 分析记录自动保存至 **「历史记录」** 标签页
- 支持展开/收起查看详情、单条删除、清空全部
- 支持批量导出为 Markdown 文件

## 注意事项

- ✅ 支持任意 http/https 网页，不限于 localhost
- 录制功能采用「录制即开关」：点「开始录制」才在当前页采集，否则不采集任何数据
- 不同站点的数据完全隔离（项目简介、录制记录、历史互不干扰）
- 所有数据存储在本地浏览器中，无需云端服务器
- 如遇到「API 密钥无效」提示，请检查设置页中的配置
- 录制前请确保在正确的页面标签页上

## 卸载方法

1. 打开 `chrome://extensions/`
2. 找到「UDA」
3. 点击「移除」按钮

> 卸载会清空所有本地存储的录制数据、配置和AI分析历史。
